---
import ServiceLayout from '../../../components/layout/service_page.astro';
import Base from '../../../components/layout/base.astro';
import { getEntry } from 'astro:content';

const entry = await getEntry("servicePage", 'content/custom-ml/index');

if (!entry) {
  throw new Error("index.md not found for custom-ml in 'servicePage' collection");
}
const indexpro = entry.data;
---

<Base>
<ServiceLayout indexpro={indexpro} /> 
</Base>
